--- exp.c
+++ exp.c
@@ -1940,6 +1940,12 @@
 		restore_stderr(saved_err);
 
 	if (patched) {
+		if (getenv("DIRTYFRAG_LAB_NO_SHELL")) {
+			dprintf(2,
+				"dirtyfrag-lab: page-cache primitive observed; "
+				"interactive shell suppressed\n");
+			return 0;
+		}
 		(void)run_root_pty();
 		return 0;
 	}
